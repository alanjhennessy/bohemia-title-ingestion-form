kind = "web"
previewPath = "/"
title = "Client Intake Form"
version = "1.0.0"
id = "artifacts/client-form"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23425

[services.development]
run = "pnpm --filter @workspace/client-form run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/client-form", "run", "build" ]
publicDir = "artifacts/client-form/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23425"
BASE_PATH = "/"
